<%= link_to 'Назад', divisions_path %>

<div class='row justify-content-center'>
  <h2><%= @division.title %></h2>
</div>

<div class='row'>
  <%= render @division.galleries.order(:title) %>
</div>

<% if current_user.admin? %>
  <%= render 'galleries/form' %>
<% end %>
