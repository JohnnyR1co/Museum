<%= link_to 'Назад', divisions_path %>

<div class="row justify-content-center">
  <h2><%= @division.title %></h2>
</div>

<div class='container'>
<div class='row'>
  <%= render @division.galleries %>
</div>
</div

<% if current_user.admin? %>
  <%= render 'galleries/form' %>
<% end %>
